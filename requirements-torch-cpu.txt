# CPU-only PyTorch wheels for Linux cloud hosts (Render).
# Install BEFORE requirements.txt:
#   pip install -r requirements-torch-cpu.txt
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.1,<3
torchvision>=0.16,<1
